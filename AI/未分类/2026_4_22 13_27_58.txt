我：阅读shiyan.md了解我需要完成的项目。现在我将要开始“阶段1”的实现。先不要生成代码,请你明确实现方法,如果有需要补充或确认的内容向我提出来。注意,关于轨迹数据的坐标映射方法可以参照sym_analyze3的实现。
claude: 
[Artifacts: undefined]
[Artifacts: undefined]
[Artifacts: undefined]
[Artifacts: undefined]
[Artifacts: undefined]
[Artifacts: undefined]
[Artifacts: undefined]
[Artifacts: undefined]
[Artifacts: undefined]
我已经仔细阅读了 `shiyan.md`（项目总览、阶段1节与特征定义表）、`sym_analyze3.py`（坐标映射与可复用函数）、`analyze.py`（笔段切分），也查看了两份样本 `s3.txt`（label=1，3个笔画，y范围 716–13629）和 `s6.txt`（label=0，2个笔画）以及 `sym_blue_mask.png`、`sym_helper_mask_completed.png`。下面是我对阶段1的实现规划和需要向你确认的事项。

---

## 一、阶段1实现思路（`features/sym_feature_extractor.py`）

### 1.1 模块职责划分

这个文件的核心使命是：**输出 7 个"原始"特征值**（0-1 比例或连续量），**不做加权、不做打分**。加权归一化留给阶段 3。

### 1.2 与 `sym_analyze3.py` 的关系

从 `sym_analyze3.py` 直接导入以下**通用基础设施**（不重复实现）：

| 导入函数 | 用途 |
|---|---|
| `read_binary_mask`, `read_user_mask_png`, `pad_to_shape`, `bbox_from_mask` | 图像/mask IO |
| `detect_helper_geometry` | 定位 axis_y、内网格线、step_x/y |
| `map_trajectory_strokes_using_reference_bbox` | **轨迹坐标映射**（按 png bbox 对齐）|
| `render_trajectory_using_reference_bbox` | 将映射后笔段渲染为二值 mask |
| `reflect_mask_across_horizontal_axis`, `reflect_strokes_across_horizontal_axis` | 关于 axis_y 翻转 |
| `distance_transform_to_mask` | 距离变换 |
| `extract_keypoints_from_target` | 网格交点关键点 |
| `_extract_target_segments_from_hough` | 霍夫线段（用于 C1）|

**不复用**的：`analyze_line_control`、`run_stage1`、`tolerant_f1`、`keypoint_coverage_score` —— 这些是带打分逻辑的包装，阶段1会用更裸的指标替代。

### 1.3 主流程

```
extract_sym_features(txt_path, png_path, blue_mask_path, helper_mask_path, out_json_path)
  ├─ 读 target_mask（蓝图）、helper_mask
  ├─ detect_helper_geometry → axis_y, inner_v/h_lines, step_x/y
  ├─ 轨迹→笔段（调用 analyze.load_trajectory_data / split_into_strokes_simple）
  │    并保留 pressure 维度（sym_analyze3 的 load_trajectory_strokes 只取 xy，C3 需要 pressure，我会另写一个带 pressure 的版本，或在流程里独立保存 pressure）
  ├─ map_trajectory_strokes_using_reference_bbox → 画布坐标下的笔段
  ├─ render → user_mask_on_canvas
  ├─ 翻转：
  │    reflected_user_mask = reflect_mask_across_horizontal_axis(user_mask, axis_y)
  │    reflected_strokes  = reflect_strokes_across_horizontal_axis(strokes_mapped, axis_y, canvas_shape)
  ├─ 构造 valid_zone：valid_zone_mask = dilate(target_mask, radius=tol_F)
  ├─ 计算 F1, F2, F3, F4, C1, C2, C3
  ├─ 组装 JSON（含 meta 诊断字段）
  └─ 返回 dict；若指定 out_json_path 则写盘
```

### 1.4 每个特征的落地细节

**F1（翻转容差 F1，0–1，↑越好）**
```
A = dilate(reflected_user_mask, r)
B = dilate(target_mask,       r)
precision = mean(B[reflected_user_mask>0])   # 用户点里有多少落在"target 容差域"
recall    = mean(A[target_mask>0])           # target 点里有多少被"用户容差域"覆盖
F1 = 2PR / (P + R + 1e-9)
```
注意：与 `sym_analyze3.tolerant_f1` 的区别是**不做 normalize_mask 归一缩放**——这会丢掉"大小差异"信息，而 F4/F3 需要原始 mask。因此 F1 按**画布原尺寸**计算 IoU 式容差指标。这里 r 默认取 `dilation_radius=5`。

**F2（关键点命中率，0–1，↑越好）**
```
keypoints = extract_keypoints_from_target(target_mask, helper, include_midpoints=False)
dist = distance_transform_to_mask(reflected_user_mask)
hit  = Σ 1[dist[y,x] <= hit_radius]   # 默认 hit_radius=6
F2 = hit / len(keypoints)
```

**F3（无效书写加权距离，≥0，↓越好）**
```
valid_zone_mask = dilate(target_mask, tol_valid)          # 允许误差的区域
dist_outside    = distanceTransform(1 - valid_zone_mask)  # 每个非 valid 像素 → valid 的最近距离
illegal = reflected_user_mask & ~valid_zone_mask
F3 = Σ dist_outside[illegal] / Σ target_mask
```
关于 shiyan.md 里"用户笔迹只考虑右半侧，越界到左侧单独记录" —— 本游戏是**水平对称轴**（axis_y），正方向是下半画布。我的默认处理：
- `user_valid_half_mask = user_mask & (y > axis_y)` → 再翻转后算 F3
- 越过轴的像素数（即原 `user_mask & (y <= axis_y)`）单独写到 `meta.cross_axis_pixels`，不纳入 F3 分子

**F4（路径偏离比，0–1，↓越好）**
```
F4 = |reflected_user_mask & ~valid_zone_mask| / |reflected_user_mask|
```

**C1（抖动比例，0–1，↓越好）**
按 shiyan.md 的**简化定义**（不用 `analyze_line_control` 的复杂版本）：
```
hough_segments = _extract_target_segments_from_hough(target_mask)
for 每条用户笔段中的每个点 p (in reflected_strokes):
    找最近的 hough 线段 (a,b) 且投影距离 <= channel_half_width（否则丢弃此点）
    residual = 垂直距离
total_pts = 有效投影点数
bad_pts   = Σ 1[residual > jitter_tol]
C1 = bad_pts / total_pts        # 不按笔段长度加权的"点级比例"
```
或者按 shiyan.md 的"按笔段长度加权平均"版本：`C1 = Σ_seg len(seg) * bad_ratio_seg / Σ_seg len(seg)`。**这一点请你在 Q3 里确认**。

**C2（短笔段比例，0–1，↓越好）**
```
stroke_lens = [ arc_length(s) for s in strokes_mapped ]
# 阈值：默认 = 画布对角线 * 0.02（更稳健；shiyan.md 提供的两个选项里我倾向这个）
thr = threshold if threshold else diag(canvas) * 0.02
short_total = Σ stroke_lens[i] for i where stroke_lens[i] < thr
C2 = short_total / Σ stroke_lens
```

**C3（压力变异系数，≥0，↓越好）**
```
all_p = concatenate([ stroke['pressure'][trim:-trim] for stroke in strokes_with_pressure if len > 2*trim ])
all_p = all_p[all_p > 0]
C3 = std(all_p) / (mean(all_p) + 1e-9)
```

### 1.5 输出 JSON（完全对齐 shiyan.md）

```json
{
  "sample_id": "s3",
  "game": "sym",
  "F1": 0.xxxx, "F2": 0.xxxx, "F3": 0.xxxx, "F4": 0.xxxx,
  "C1": 0.xxxx, "C2": 0.xxxx, "C3": 0.xxxx,
  "meta": {
    "num_strokes": 3,
    "total_points": 13299,
    "axis_y": 800,
    "step_x": 96, "step_y": 96,
    "num_keypoints": 12,
    "keypoints_hit": 10,
    "cross_axis_pixels": 52,
    "canvas_hw": [1601, 1201],
    "params": {"dilation_F1": 5, "tol_valid": 9, "hit_radius": 6, "jitter_tol": 3.0, "C2_thr_ratio": 0.02, "C3_trim_ends": 3}
  }
}
```

---

## 二、需要你确认的事项

在动手写代码之前，有几个点会直接影响实现，希望你先明确一下：

### Q1. **缺失 `.png` 对齐参照文件 ⚠️ 最关键**
你上传了 `s3.txt` 和 `s6.txt`，但**没有对应的 `s3.png` / `s6.png`**。而 `sym_analyze3.map_trajectory_strokes_using_reference_bbox` 依赖 png 的 bbox 来对齐轨迹坐标到画布。

我的三种备选方案：

- **(a) 坚持原方案**：你稍后会提供每份样本对应的 `.png`（由电子笔软件导出或你之前的渲染脚本产生）。接口签名保留 `png_path`。
- **(b) 退化为蓝图反射 bbox 对齐**：把 `reflected_target_bbox` 作为目的 bbox，轨迹 bbox 映射过去。**缺点**：用户画得大/小的"大小差异"信息会被抹掉，F1、F4 失去一部分鉴别力。
- **(c) 设备坐标直接缩放**：假设轨迹原始坐标域固定（如 0–14000 → canvas 1201×1601 做线性缩放）。**需要你确认电子笔的设备坐标范围**。

**→ 请选 (a) / (b) / (c)。若选 (a)，需要补上 png 文件再测试。**

### Q2. **F3 的"右半侧"在水平对称游戏下的解读**
shiyan.md 写的是"用户笔迹只考虑右半侧（对称轴右侧）"，但当前游戏是**水平对称轴**（axis_y 横线）。
- 我的计划解读：把"右半侧"理解为"**用户本应书写的那一半**"，即 `y > axis_y` 的下半区。越界到 `y <= axis_y`（即画到蓝图区域上去）的像素数量单独记录到 `meta.cross_axis_pixels`，不参与 F3 的分子加权。
- 是否同意？或者你希望把越轴像素也加权计入 F3（例如乘一个惩罚系数）？

### Q3. **C1 的加权方式**
shiyan.md 括号里写"（按笔段长度加权平均）"。两种实现：
- **(A) 点级**：`C1 = bad_points / total_points`（所有霍夫线段上的投影点一起算）
- **(B) 段级加权**：`C1 = Σ len(seg_i) * bad_ratio_i / Σ len(seg_i)`

**→ 选 A 还是 B？**（A 更简单，B 更契合"按长度加权"字面意思）

### Q4. **容差半径参数初值**
shiyan.md 给了 `dilation_radius=5 (F1)`, `hit_radius=6 (F2)`, `jitter_tol=3.0 (C1)`，但 F3/F4 的 `valid_zone_mask` 膨胀半径没明确给。
- 我的默认：`tol_valid = 9`（沿用 `tolerant_f1` 里的 9 像素容差，体感合理）。
- 或者你希望自适应 `tol_valid = max(9, step_y * 0.12)`（与 helper 网格步长挂钩，更鲁棒）？

### Q5. **C2 阈值默认策略**
- **(a)** 固定：画布对角线 × 2%（对短样本更稳定，**我推荐**）
- **(b)** 自适应：本样本所有笔段长度的 25 分位数（笔段很少时不稳定）
- **(c)** 两者取较小值

### Q6. **sym_analyze3 的导入路径**
按 shiyan.md 的项目结构，`features/sym_feature_extractor.py` 在 `features/` 子目录，而 `sym_analyze3.py` 在项目根。导入方式：
```python
import sys
sys.path.insert(0, str(Path(__file__).parent.parent))
from sym_analyze3 import (...)
```
如果你已有更清爽的方式（例如把 sym_analyze3 放进 package），请告诉我。

### Q7. **压力数据的获取**
`sym_analyze3.load_trajectory_strokes` 返回的 stroke 只有 `[x, y]`，丢了 pressure。而 C3 需要 pressure。
- 我的计划：**不改** sym_analyze3，而是在 sym_feature_extractor 里另写一个 `load_trajectory_strokes_with_pressure()`（基于 `analyze.split_into_strokes_simple`，返回含 pressure 的 dict 列表），和映射后的 xy 笔段**并列保存**（xy 用于空间特征、pressure 用于 C3，两者通过"同一笔段 index"对应）。

这样可以吗？还是你希望我在 sym_analyze3 里直接加一个 `load_trajectory_strokes_xyp`？

---

等你回复以上 7 点，我就可以开写代码。**其中 Q1 最关键**（决定了坐标对齐是否能跑通），其他都是小参数/风格问题。
我：ANS1：(a)坚持原方案
ANS2：我希望把越轴像素也加权计入 F3，乘一个惩罚系数
ANS3：选A
ANS4：使用tol_valid = 9
ANS5：(a) 固定：画布对角线 × 2%
ANS6：把 sym_analyze3 放进 package
ANS7：我同意你的计划
开始写代码吧
claude: 
好，所有问题都确认了。开始写代码。我先按你的要求把项目结构搭起来（包括把 `sym_analyze3` 放进 package），然后写 `features/sym_feature_extractor.py`，并做能做的静态验证。

## 一、搭建项目结构
[Artifacts: undefined]
[Artifacts: undefined]
[Artifacts: undefined]
[Artifacts: undefined]
[Artifacts: undefined]
Now let me check that `sym_analyze3.py` still imports correctly after being placed in a package:
[Artifacts: undefined]
好，包结构可用。先探测一下 helper mask 的关键参数（axis_y 和网格步长），确保计算按预期工作：
[Artifacts: undefined]
一切就绪：canvas 1601×1201，axis_y=794，网格步长 ~53px，目标上有 28 个关键点、11 条霍夫线段。现在写主文件：
[Artifacts: undefined]
## 二、静态 & 单元验证

先做导入和语法检查，然后对能独立测试的子函数（C3 不需要坐标映射、F1/F2/F4 可用合成 mask）做单元验证：
[Artifacts: undefined]
[Artifacts: undefined]
[Artifacts: undefined]
[Artifacts: undefined]
所有单元测试都通过了，关键验证结果：

- **F1-F4** 在合成场景下方向正确：相同→1.0、不相交→0.0、越轴像素确实被 cross_penalty 惩罚（`cross_contrib = 10 × 100 / 200 = 5.0` ✓）
- **C1** 点级逻辑正确：贴线→0、全偏移 5px→1.0、通道外的点不纳入投影
- **C2** 数值精确匹配：`3/203 = 0.0148` ✓
- **C3 在真实数据上方向符合预期**：`s3 (label=1) C3=0.188 > s6 (label=0) C3=0.118` — 障碍样本压力变异大，符合"↓越好"的方向

再加一个 `shiyan.md` 里指定的**可视化诊断叠加图**函数（便于拿到 png 后目视验证坐标对齐）：
[Artifacts: undefined]
现在做一个**模拟端到端测试**：由于用户暂未提供 png，我用 s3/s6 的轨迹 bbox 作 png 参照（把 png 的角色降级为"就是轨迹自己"），跑通整个流程，验证无异常：
[Artifacts: undefined]
[Artifacts: undefined]
端到端流水线跑通。C3 在真实压力数据上方向正确（s3=0.188 > s6=0.118），空间类特征数值异常是因为 mock png 放置随意——这正是为什么 shiyan.md 强调必须用真实电子笔 png。看一眼诊断图确认可视化逻辑：
[Artifacts: undefined]
可视化效果符合预期：
- **蓝色** = 目标图形（蓝图 + 灰色容差边带）
- **白色** = 翻转后的用户笔迹（用户轨迹形状确实是类似的山形 —— 证明 s3 是在尝试对称绘制）
- **黄色横线** = 对称轴 axis_y=794
- **红色小圈** = 未命中的关键点（mock png 位置不准，所以全红，合预期）

一旦你提供真实 png，此图就能用来目视校准坐标对齐。

## 三、交付文件
[Artifacts: undefined]
[Artifacts: undefined]
## 四、落地与使用说明

**1. 项目结构调整**（按你选的 ANS6，把 `sym_analyze3` 放进 package）

```
project_root/
├── sym_core/
│   ├── __init__.py          ← 用 sym_core__init__.py 重命名后放入
│   └── sym_analyze3.py      ← 从项目根移动过来
├── pen/
│   ├── __init__.py          （空文件即可）
│   └── analyze.py           （已有）
└── features/
    ├── __init__.py          （空文件即可）
    └── sym_feature_extractor.py   ← 新文件
```

**2. 调用方式**

```python
from features.sym_feature_extractor import extract_sym_features, visualize_sym_extraction

res = extract_sym_features(
    txt_path="data/samples/sym/s3.txt",
    png_path="data/samples/sym/s3.png",           # ← 需要你提供
    blue_mask_path="output_sym/shape_sym/sym_blue_mask.png",
    helper_mask_path="output_sym/shape_sym/sym_helper_mask_completed.png",
    out_json_path="features/s3_features.json",
)
# res 含 sample_id, game, F1-F4, C1-C3, meta

# 可选：生成目视校准叠加图
visualize_sym_extraction(..., out_png_path="diag/s3_overlay.png")
```

或命令行：

```bash
python -m features.sym_feature_extractor \
    --txt data/samples/sym/s3.txt \
    --png data/samples/sym/s3.png \
    --blue output_sym/shape_sym/sym_blue_mask.png \
    --helper output_sym/shape_sym/sym_helper_mask_completed.png \
    --out features/s3.json --vis diag/s3_overlay.png
```

## 五、已完成的验证

| 验证项 | 结果 |
|---|---|
| 模块导入（跨 package：sym_core, pen, features）| ✓ |
| F1 合成 mask：相同→1.0、不相交→0.0、半重叠→0.41（居中）| ✓ |
| F2 关键点命中：全在内→1.0、全在外→0.0 | ✓ |
| F3 三场景：正确→0、远离→只走 main 通道、越轴→走 cross 通道（按 `10×n_cross/target_area` 惩罚）| ✓ |
| F4 on/off 比例 | ✓ |
| C1 点级：贴线→0、全偏移 5px→1.0、通道外→ total=0 | ✓ |
| C2 数值精确匹配手算结果 | ✓ |
| C3 合成恒压力→0，变化压力→0.32 | ✓ |
| **真实数据 C3 方向性**：`s3 (label=1) = 0.188 > s6 (label=0) = 0.118` | ✓ 符合↓越好 |
| 端到端流水线（用模拟 png）：无崩溃，7 个特征全部输出 | ✓ |
| 可视化叠加图生成 | ✓ |

## 六、你需要做的事

1. **补齐 png 文件**：把每份 `.txt` 样本对应的 `.png`（电子笔设备产出的那个）放进 `data/samples/sym/`。**这是跑出真实空间特征值的前提**。
2. **重命名/放置** `sym_core__init__.py` → `sym_core/__init__.py`，并把项目根的 `sym_analyze3.py` 移进 `sym_core/`。
3. **跑一次真实样本**（比如 s3 + s3.png），把输出 JSON 和 `_overlay.png` 发我，我们目视校准坐标对齐，确认 F1~F4 落在 `shiyan.md` §4.1 给出的"预期结果"范围内（正常样本 F1>0.6，F2>0.7 等）。
4. 若校准结果合理，就可以进入**阶段 2**（迷宫特征提取器），届时可以把 C1/C2/C3 的实现提炼到 `features/stroke_utils.py` 作为公共库。
