# 阶段一实现文档：对称绘图游戏特征提取器

## 项目概述

阶段一的核心任务是实现对称绘图游戏（sym）的特征提取模块 `features/sym_feature_extractor.py`，该模块负责从用户的绘图轨迹数据中提取 **7 个原始特征值**（4 个形状相关特征 F1-F4，3 个笔迹控制特征 C1-C3），为后续的评分和障碍检测提供数据基础。

## 核心设计原则

### 职责划分
- **特征提取模块**：只输出 0-1 比例或连续量的原始特征值
- **不做加权、不做打分**：加权归一化留给阶段 3
- **完全对齐规范**：输出 JSON 格式与 shiyan.md 规范完全一致

### 模块依赖关系

从 `sym_core/sym_analyze3.py` 导入以下基础设施（不重复实现）：

| 导入函数 | 用途 |
|---------|------|
| `read_binary_mask`, `read_user_mask_png`, `pad_to_shape`, `bbox_from_mask` | 图像/mask IO 操作 |
| `detect_helper_geometry` | 定位 axis_y、内网格线、step_x/y |
| `map_trajectory_strokes_using_reference_bbox` | **轨迹坐标映射**（按 png bbox 对齐）|
| `render_trajectory_using_reference_bbox` | 将映射后笔段渲染为二值 mask |
| `reflect_mask_across_horizontal_axis`, `reflect_strokes_across_horizontal_axis` | 关于 axis_y 翻转 |
| `distance_transform_to_mask` | 距离变换 |
| `extract_keypoints_from_target` | 网格交点关键点 |
| `_extract_target_segments_from_hough` | 霍夫线段（用于 C1）|

**不复用的**：`analyze_line_control`、`run_stage1`、`tolerant_f1`、`keypoint_coverage_score` —— 这些是带打分逻辑的包装，阶段一用更裸的指标替代。

## 项目结构调整

将 `sym_analyze3` 放进 package：

```
project_root/
├── sym_core/
│   ├── __init__.py          # 导出所有公共函数
│   └── sym_analyze3.py      # 从项目根移动过来
├── pen/
│   ├── __init__.py          # 空文件
│   └── analyze.py           # 笔段切分功能
└── features/
    ├── __init__.py          # 空文件
    └── sym_feature_extractor.py   # 阶段一核心模块
```

## 主流程设计

```
extract_sym_features(txt_path, png_path, blue_mask_path, helper_mask_path, out_json_path)
  ├─ 读 target_mask（蓝图）、helper_mask
  ├─ detect_helper_geometry → axis_y, inner_v/h_lines, step_x/y
  ├─ 轨迹→笔段（调用 analyze.load_trajectory_data / split_into_strokes_simple）
  │    并保留 pressure 维度（用于 C3 特征）
  ├─ map_trajectory_strokes_using_reference_bbox → 画布坐标下的笔段
  ├─ render → user_mask_on_canvas
  ├─ 翻转：
  │    reflected_user_mask = reflect_mask_across_horizontal_axis(user_mask, axis_y)
  │    reflected_strokes  = reflect_strokes_across_horizontal_axis(strokes_mapped, axis_y, canvas_shape)
  ├─ 构造 valid_zone：valid_zone_mask = dilate(target_mask, radius=tol_F)
  ├─ 计算 F1, F2, F3, F4, C1, C2, C3
  ├─ 组装 JSON（含 meta 诊断字段）
  └─ 返回 dict；若指定 out_json_path 则写盘
```

## 七个特征的详细实现

### F1：翻转容差 F1（0-1，↑越好）

**定义**：使用容差膨胀后的 F1 分数，衡量翻转后的用户笔迹与目标图形的重叠程度。

```python
A = dilate(reflected_user_mask, r)  # 用户笔迹膨胀
B = dilate(target_mask, r)          # 目标图形膨胀
precision = mean(B[reflected_user_mask>0])   # 用户点里有多少落在"target 容差域"
recall    = mean(A[target_mask>0])           # target 点里有多少被"用户容差域"覆盖
F1 = 2PR / (P + R + 1e-9)
```

**关键参数**：
- `dilation_radius = 5` 像素

**与 sym_analyze3.tolerant_f1 的区别**：
- **不做 normalize_mask 归一缩放**，保留"大小差异"信息
- 按**画布原尺寸**计算 IoU 式容差指标

### F2：关键点命中率（0-1，↑越好）

**定义**：网格交点关键点被用户笔迹覆盖的比例。

```python
keypoints = extract_keypoints_from_target(target_mask, helper, include_midpoints=False)
dist = distance_transform_to_mask(reflected_user_mask)
hit  = Σ 1[dist[y,x] <= hit_radius]
F2 = hit / len(keypoints)
```

**关键参数**：
- `hit_radius = 6` 像素

### F3：无效书写加权距离（≥0，↓越好）

**定义**：用户笔迹偏离有效区域的加权距离，包含越轴惩罚。

```python
valid_zone_mask = dilate(target_mask, tol_valid)          # 允许误差的区域
dist_outside    = distanceTransform(1 - valid_zone_mask)  # 每个非 valid 像素 → valid 的最近距离

# 主要违规区域（下半画布的越界部分）
illegal_main = reflected_user_mask & (y > axis_y) & ~valid_zone_mask
main_contrib = Σ dist_outside[illegal_main]

# 越轴惩罚（画到上半画布的部分）
cross_axis_pixels = reflected_user_mask & (y <= axis_y)
cross_contrib = cross_penalty * count(cross_axis_pixels)

F3 = (main_contrib + cross_contrib) / Σ target_mask
```

**关键参数**：
- `tol_valid = 9` 像素（ANS4）
- `cross_penalty = 10`（越轴像素的惩罚系数，ANS2）

**设计说明**：
- 对称轴为**水平线** axis_y
- 用户应在下半画布（y > axis_y）书写
- 越界到上半画布的像素会被额外惩罚

### F4：路径偏离比（0-1，↓越好）

**定义**：用户笔迹中偏离有效区域的像素比例。

```python
F4 = |reflected_user_mask & ~valid_zone_mask| / |reflected_user_mask|
```

### C1：抖动比例（0-1，↓越好）

**定义**：笔段点偏离霍夫线段的比例（点级统计）。

```python
hough_segments = _extract_target_segments_from_hough(target_mask)
for 每条用户笔段中的每个点 p (in reflected_strokes):
    找最近的 hough 线段 (a,b) 且投影距离 <= channel_half_width
    （否则丢弃此点）
    residual = 垂直距离
total_pts = 有效投影点数
bad_pts   = Σ 1[residual > jitter_tol]
C1 = bad_pts / total_pts
```

**关键参数**：
- `jitter_tol = 3.0` 像素
- `channel_half_width = 20` 像素

**加权方式**：**点级统计**（ANS3 选择 A）
- 所有霍夫线段上的投影点一起算
- 不按笔段长度加权

### C2：短笔段比例（0-1，↓越好）

**定义**：短笔段总长度占所有笔段总长度的比例。

```python
stroke_lens = [arc_length(s) for s in strokes_mapped]
# 阈值：画布对角线 * 0.02（ANS5 选择方案 a）
thr = diag(canvas) * 0.02
short_total = Σ stroke_lens[i] for i where stroke_lens[i] < thr
C2 = short_total / Σ stroke_lens
```

**关键参数**：
- `C2_thr_ratio = 0.02`（固定比例）

### C3：压力变异系数（≥0，↓越好）

**定义**：笔段压力值的变异系数（标准差/均值）。

```python
all_p = concatenate([
    stroke['pressure'][trim:-trim] 
    for stroke in strokes_with_pressure 
    if len > 2*trim
])
all_p = all_p[all_p > 0]
C3 = std(all_p) / (mean(all_p) + 1e-9)
```

**关键参数**：
- `C3_trim_ends = 3`（裁剪笔段两端的点数）

**数据获取**（ANS7）：
- 不改 sym_analyze3，在 sym_feature_extractor 里另写 `load_trajectory_strokes_with_pressure()`
- 基于 `analyze.split_into_strokes_simple`，返回含 pressure 的 dict 列表
- xy 用于空间特征、pressure 用于 C3，两者通过"同一笔段 index"对应

## 输出 JSON 格式

完全对齐 shiyan.md 规范：

```json
{
  "sample_id": "s3",
  "game": "sym",
  "F1": 0.xxxx,
  "F2": 0.xxxx,
  "F3": 0.xxxx,
  "F4": 0.xxxx,
  "C1": 0.xxxx,
  "C2": 0.xxxx,
  "C3": 0.xxxx,
  "meta": {
    "num_strokes": 3,
    "total_points": 13299,
    "axis_y": 800,
    "step_x": 96,
    "step_y": 96,
    "num_keypoints": 12,
    "keypoints_hit": 10,
    "cross_axis_pixels": 52,
    "canvas_hw": [1601, 1201],
    "params": {
      "dilation_F1": 5,
      "tol_valid": 9,
      "hit_radius": 6,
      "jitter_tol": 3.0,
      "C2_thr_ratio": 0.02,
      "C3_trim_ends": 3
    }
  }
}
```

## 坐标映射方案

**选择方案 (a)**（ANS1）：坚持原方案，使用真实 png 文件

- 每个样本需要提供对应的 `.png` 文件（由电子笔软件导出）
- 使用 `map_trajectory_strokes_using_reference_bbox` 依赖 png 的 bbox 来对齐轨迹坐标到画布
- 接口签名保留 `png_path` 参数

**优势**：
- 保留用户画得大/小的"大小差异"信息
- F1、F4 保持完整的鉴别力

## 可视化诊断功能

实现了 `visualize_sym_extraction()` 函数，生成叠加图用于目视校准坐标对齐：

**颜色编码**：
- **蓝色**：目标图形（蓝图 + 灰色容差边带）
- **白色**：翻转后的用户笔迹
- **黄色横线**：对称轴 axis_y
- **红色小圈**：未命中的关键点

## 验证结果

### 单元测试验证

| 验证项 | 结果 |
|-------|------|
| 模块导入（跨 package：sym_core, pen, features）| ✓ |
| F1 合成 mask：相同→1.0、不相交→0.0、半重叠→0.41 | ✓ |
| F2 关键点命中：全在内→1.0、全在外→0.0 | ✓ |
| F3 三场景：正确→0、远离→只走 main 通道、越轴→走 cross 通道（按 10×n_cross/target_area 惩罚）| ✓ |
| F4 on/off 比例 | ✓ |
| C1 点级：贴线→0、全偏移 5px→1.0、通道外→ total=0 | ✓ |
| C2 数值精确匹配手算结果：3/203 = 0.0148 | ✓ |
| C3 合成恒压力→0，变化压力→0.32 | ✓ |

### 真实数据方向性验证

C3 特征在真实数据上方向符合预期：
- `s3 (label=1，有障碍) C3 = 0.188`
- `s6 (label=0，无障碍) C3 = 0.118`
- **符合"↓越好"的方向**（障碍样本压力变异更大）

### 端到端流程验证

- 使用模拟 png 完成端到端流水线测试：无崩溃，7 个特征全部输出 ✓
- 可视化叠加图生成成功 ✓

## 使用方法

### Python API 调用

```python
from features.sym_feature_extractor import extract_sym_features, visualize_sym_extraction

res = extract_sym_features(
    txt_path="data/samples/sym/s3.txt",
    png_path="data/samples/sym/s3.png",
    blue_mask_path="output_sym/shape_sym/sym_blue_mask.png",
    helper_mask_path="output_sym/shape_sym/sym_helper_mask_completed.png",
    out_json_path="features/s3_features.json",
)
# res 含 sample_id, game, F1-F4, C1-C3, meta

# 可选：生成目视校准叠加图
visualize_sym_extraction(..., out_png_path="diag/s3_overlay.png")
```

### 命令行调用

```bash
python -m features.sym_feature_extractor \
    --txt data/samples/sym/s3.txt \
    --png data/samples/sym/s3.png \
    --blue output_sym/shape_sym/sym_blue_mask.png \
    --helper output_sym/shape_sym/sym_helper_mask_completed.png \
    --out features/s3.json --vis diag/s3_overlay.png
```

## 关键决策汇总

| 问题 | 决策 | 说明 |
|------|------|------|
| Q1: png 对齐参照文件 | (a) 坚持原方案 | 需要提供每个样本对应的真实 png 文件 |
| Q2: F3 的越轴处理 | 加权计入，惩罚系数=10 | 越轴像素会被额外惩罚 |
| Q3: C1 加权方式 | 选 A（点级统计）| `C1 = bad_points / total_points` |
| Q4: valid_zone 膨胀半径 | tol_valid = 9 | 固定 9 像素容差 |
| Q5: C2 阈值策略 | (a) 画布对角线 × 2% | 固定比例，更稳定 |
| Q6: 项目结构 | 把 sym_analyze3 放进 package | 形成 sym_core 包 |
| Q7: 压力数据获取 | 单独实现 load_trajectory_strokes_with_pressure | 不改 sym_analyze3 |

## 下一步工作

1. **补齐 png 文件**：将每个 `.txt` 样本对应的 `.png` 放入 `data/samples/sym/`
2. **真实样本验证**：跑一次真实样本（如 s3 + s3.png），目视校准坐标对齐
3. **确认特征范围**：确认 F1~F4 落在预期结果范围内（正常样本 F1>0.6，F2>0.7 等）
4. **进入阶段 2**：实现迷宫特征提取器，并将 C1/C2/C3 提炼到 `features/stroke_utils.py` 作为公共库

## 技术要点

### 水平对称轴处理
- 游戏使用**水平对称轴**（axis_y 横线）
- 用户应在**下半画布**（y > axis_y）书写
- "右半侧"理解为"用户本应书写的那一半"

### 容差机制
- F1 使用膨胀容差计算重叠度
- F3/F4 使用 valid_zone_mask 定义允许误差的区域
- 不同特征使用不同的容差半径

### 压力数据处理
- 裁剪笔段两端避免起收笔压力异常
- 过滤零压力值
- 使用变异系数归一化不同压力范围

### 霍夫线段投影
- C1 只统计能投影到霍夫线段 channel_half_width 内的点
- 通道外的点不纳入计算
- 使用点到线段的垂直距离判断抖动