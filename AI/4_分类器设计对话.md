# 我

阅读shiyan_v3.0.md可以看到项目介绍、当前我的实验进度与方案设计等信息。目前我已经完成了阶段3及之前的代码实现，成功提取了圆形迷宫、方形迷宫和对称图形的多维特征。在开始阶段4代码实现前，我想先确定实验设计，再开始使用这些特征训练轻量级分类器。

已知三个游戏的样本数据情况如下。样本数量比较小，但这里更多的是研究方法论，所以样本仅供测试方法可行性用，不需要考虑小样本数据集得到的结果是否有代表性或数据集中的样本是否有代表性。在这些样本中有一些完全偏离绘制目标的乱画的样本，需要考虑怎么处理这一部分样本，使得分类结果更可信。

标注数据表labels.csv每行格式为{sample_id,game,label}，label=0表示正常，label=1表示书写障碍，乱画样本在表中label为1。

```

game,正常非乱画个数,书写障碍非乱画个数,乱画样本个数,{乱画样本id}

sym,4,3,1,{s4}

maze,11,18,5,{l15,l16,l17,l29,l31}

circle,5,10,6,{c3,c6,c8,c18,c19,c21}

```

已知三个游戏均有F1~F4、C1~C3这七个特征。这七个特征的在不同的游戏中衡量的指标抽象意义上是相同的，但部分特征的具体含义与游戏类型关联。其中F1~F4是与具体游戏关联的特征：例如F1代表的是任务完成度，在对称游戏中是用户笔迹与对称后的原图形的重叠程度（翻转容差），在迷宫游戏中是用户笔迹对目标解通道的覆盖率；F2为关键结构对齐度，在对称游戏中为网格交点的命中率，在迷宫中为解路径采样点命中率。而C1~C3不与游戏类型挂钩，例如C3表示压力变异系数，无论在哪个游戏中都是衡量笔段压力值变化的特征。因此，同一特征在不同游戏中的值的情况可能差别很大。例如对于同一个测试者来说，对称游戏的绘制目标会比迷宫游戏更加明确，尤其是迷宫绘制过程中可能出现走错路而“原路返回”的情况，导致迷宫游戏的F3与F4特征值很可能普遍大于对称游戏；又如圆形迷宫笔迹中多为弧线，方形迷宫笔迹相较更加笔直，如果一种线理论上绘制难度大于另一种，那其中一种对应的抖动系数C1可能会大于另一种。在本项目中我们不需要研究不同游戏的同一特征值的差异的原因，我们需要的是设计一个方法，通过这个方法将不同游戏（即不同标准下）的同一特征项的值映射到同一标准尺度下，然后利用处理后的特征值训练一个二分类分类器，分辨。类似的抽象情况是，1月、2月、3月的三次语文、数学、英语考试难度不同，需要设计一个分类器，无论是输入哪个月哪门考试的成绩，都能判断这个成绩是不是优秀的。

对于这个分类器输入的各种特征指标，我认为它们重要性并不平等。这是否可以作为分类器的一个设计思路？

现在请你结合我已经完成的实验、数据和项目本身的特点等信息，设计从特征抽取到落地分类的实验方案。我希望这个分类器的设计是尽量简单且有效的。 如果有需要补充或确认的内容向我提出来。

# claude

我先读取文件，了解项目的完整背景和你已经完成的工作。好的，已了解项目全貌。我再看一下中间被截断的阶段2（方形迷宫）部分，确认方形迷宫F1~C3的定义，这样设计归一化方案时才准确。好，我已经完整理解了项目。特征定义、样本情况和你的顾虑都清楚了。在给方案之前，有几个关键点我想先澄清清楚——这几个决定会直接决定整个阶段4/5的设计走向：

## 需要与你确认的关键问题

### 问题1：乱画样本的定位
你把乱画样本（s4、l15-l31中的5个、c3-c21中的6个）的 label 标记为 1（书写障碍）。这里有一个根本性的设计选择需要明确：

**乱画是否应作为"书写障碍"的一个子类参与训练？**

- **观点A（纳入训练）**：乱画样本本身就是异常绘制行为，在真实筛查场景下，如果一个孩子完全放弃任务、画得一塌糊涂，这种"不配合/无法完成任务"本身就是书写障碍的一种行为表现，应该被识别为异常。
- **观点B（作为离群点剔除）**：乱画样本在特征空间中极度偏离所有其他样本（F1/F2会接近0，F3/F4会极大），会污染决策边界，让分类器学到"只要画得离谱就是书写障碍"的捷径，而不是学到真正区分正常/障碍的细微特征。
- **观点C（折中：两阶段筛查）**：先用一个简单的"可分析性门控"把乱画样本挑出来直接判为异常（不参与主分类器训练），然后主分类器只在"认真画了但画得好/不好"的样本上做精细区分。

我的倾向是**观点C**。理由：乱画样本在F1/F2上可能已经低到0.05以下，保留它们会让归一化统计量（均值、分位数）被严重拉偏，反而伤害正常样本的区分度。而且从方法论上，"先过滤掉不可分析样本，再在可分析样本上精细判别"本身就是医学筛查的标准范式。

### 问题2：跨游戏归一化的"锚"
把三个游戏的同一特征映射到同一尺度，核心问题是：**用谁做基准？** 有三种思路：

- **A. 按游戏内分布归一（per-game z-score / robust scaling）**：每个游戏单独算自己的统计量（中位数、MAD），把特征转成"该样本在本游戏内相对位置"。优点：天然消除游戏间难度差异；缺点：归一化时混入了全部样本（含书写障碍），统计量不"干净"。
- **B. 按游戏内"正常样本"归一**：每个游戏单独算 label=0 样本的统计量作为基准，所有样本（含label=1）都按"偏离正常的程度"来量化。优点：语义清晰——归一后的值就是"相对正常儿童的偏离量"；缺点：需要已知标签，不能用于纯无监督场景（但你是监督学习，没问题）。
- **C. 分位数归一（quantile normalization）**：把每个游戏的特征分布强制映射到同一分布（如标准正态）。优点：彻底消除分布形状差异；缺点：样本量小的时候分位数估计不稳定。

我推荐 **方案B（按游戏内正常样本做robust z-score）**，理由是它和你想做的"跨游戏/跨难度映射到同一判别尺度"的直觉最吻合，而且在小样本下稳定——中位数和MAD比均值/方差稳健得多。这对应你那个类比："先知道每门考试的'正常水平线'在哪，再看每个学生偏离多少"。

### 问题3：特征重要性如何进入分类器
你提到"特征重要性不平等是否可作为设计思路"——**是的，而且应该是**。在小样本（每游戏约15-30个，且类别不平衡）下，让模型自己从数据中学权重很容易过拟合。更稳的做法是**半先验权重**：

- F轴（任务完成度类）在功能层面更直接反映"能不能完成目标"，先验权重应略高；
- C轴（过程控制类）反映运动控制细节，作为补充；
- 在F轴内部，F1/F2（正向，完成度）比F3/F4（负向，偏离度）表达的信息更稳定——F1/F2 有明确上界1，F3会因为样本大小不同波动，信息量略低。

### 问题4：分类器选型
基于小样本（约50+条）、7维特征、二分类、你要求"简单且有效"、且希望体现特征重要性，我推荐的优先级是：

1. **加权线性评分 + 阈值**（baseline，最简单）：归一化后按半先验权重加权求和，留一法交叉验证（LOO）确定阈值。
2. **L2正则化Logistic Regression**：让数据微调权重，但正则化避免过拟合；系数天然提供重要性解释。
3. **Random Forest / 浅层Gradient Boosting**（只作为对照）：看能否从非线性/交互中拿到额外收益。

**简单且有效**的主角应当是**前两个**，第三个只在报告中作为"更复杂模型并未显著优于简单模型"的对比项，反衬方法论简洁性的合理。

---

想请你先对以下四点给我答复，我再据此给出完整的阶段4/5方案：等待你选完这四个选项再出完整方案。选完之后我会给你一个包含阶段4（特征汇总+归一化）、阶段5（分类器）、阶段6（评估）的完整设计，包括每个程序的接口、核心公式和交叉验证协议。

# 我

Q: 乱画样本怎么处理？
A: 方案C：两阶段——先门控剔除乱画直接判异常，主分类器只在可分析样本上训练（推荐）

Q: 跨游戏归一化基准？
A: 方案B：按游戏内label=0样本的中位数/MAD做robust z-score（推荐）

Q: 特征权重策略？
A: 半先验和纯先验对比

Q: 分类器选型？
A: 双主角：加权线性评分(baseline) + L2 Logistic Regression(主力)，RF仅作对照（推荐）

# claude

好，方案完全定版。下面给你v3.1文档中**阶段4/5/6部分的完整内容**，可以直接替换原文档中"从此处开始为代码未完成、设计待修改的部分"之后的所有内容。

---

## 阶段4：特征汇总 + 归一化管道

### 4.4.0 模块总览

```
per-game JSON（阶段1-3已有）
      ↓
【4.A】gate_unanalyzable.py  乱画门控
      ↓
      ├─ 可分析样本 → 进入4.B
      └─ 乱画样本 → 旁路到阶段6终判（直接 prob=1.0）
      ↓
【4.B】normalize.py  per-game robust z-score
  （基准：本游戏内 label=0 且可分析 的样本的 median/MAD）
      ↓
【4.C】build_feature_matrix.py  主程序（A→B的编排 + 输出）
      ↓
feature_matrix.csv + gate_decisions.csv + normalize_stats.json
```

**核心原则**：
- 门控采用无监督、可解释的硬规则（不训练门控分类器，避免循环论证）。
- 归一化统计量只在 `label=0 且 可分析` 的样本上估计，让 z-score 的语义明确为"相对正常儿童的偏离量"。
- 所有归一化后的特征方向统一为"越大越异常"，便于后续加权评分。

---

### 4.4.1 阶段4.A：乱画门控 `features/gate_unanalyzable.py`

**调用**：
```bash
python features/gate_unanalyzable.py \
    --json_dirs output_sym/extract output_maze/extract output_circle/extract \
    --labels data/labels.csv \
    --config configs/gate_thresholds.yaml \
    --out output/gate_decisions.csv \
    --report output/gate_validation_report.txt
```

**门控规则**（满足任一即判为"不可分析"）：

| 规则 | 判据 | 初始阈值（sym / maze / circle） | 语义 |
|---|---|---|---|
| G1 | F1 < τ₁ | 0.10 / 0.08 / 0.08 | 任务完成度低到无意义 |
| G2 | F4 > τ₂ | 0.70 / 0.70 / 0.70 | 笔迹大部分落在有效区外 |
| G3 | F1 + F2 < τ₃ | 0.20 / 0.15 / 0.15 | 两个完成度指标同时塌陷 |

**配置文件** `configs/gate_thresholds.yaml`：
```yaml
sym:    {tau_F1: 0.10, tau_F4: 0.70, tau_F1F2: 0.20}
maze:   {tau_F1: 0.08, tau_F4: 0.70, tau_F1F2: 0.15}
circle: {tau_F1: 0.08, tau_F4: 0.70, tau_F1F2: 0.15}
```

**阈值确定流程（两轮迭代）**：

1. **第一轮**：用上表初始阈值跑全部样本，生成 `gate_decisions.csv` 和验证报告。
2. **验证报告内容**（`gate_validation_report.txt`）：
   - 12 个已知乱画样本的 F1/F2/F4 实际值 + 每条触发了哪些规则；
   - 召回率（12 个乱画中被识别出几个）、精度（被识别出的里有多少是真乱画）；
   - 所有 `label=0` 样本的 F1/F2/F4 最值，对照阈值——确认是否有正常样本被误判。
3. **第二轮回调**：
   - 若有已知乱画样本未被识别 → 放宽阈值到"刚好能覆盖它"；
   - 若有 label=0 样本被误判 → 收紧阈值到"刚好能排除它"；
   - 若两者冲突（门控无法完美分离）→ 接受冲突，在报告中明确记录，并把冲突样本的行为交由主分类器判断（此时这些样本仍会进入后续流程）。

**输出 `gate_decisions.csv`**：
```csv
sample_id,game,label,F1,F2,F4,is_unanalyzable,triggered_rules
s1,sym,0,0.72,0.81,0.18,False,
s4,sym,1,0.04,0.05,0.92,True,G1|G2|G3
l15,maze,1,0.05,0.03,0.88,True,G1|G2|G3
...
```

**验收标准**：
- 12 个已知乱画样本召回率 = 100%
- label=0 样本误判数 = 0
- 若无法同时满足，报告中列出冲突并说明处理方案

---

### 4.4.2 阶段4.B：归一化 `features/normalize.py`

**函数接口**（设计成 `fit / transform` 分离，便于阶段6的LOSO每折重新拟合）：

```python
def fit_normalize_stats(
    feature_dicts: List[dict],   # 可分析 + label=0 的样本特征dict列表
    games: List[str],
    feature_names: List[str] = ['F1','F2','F3','F4','C1','C2','C3'],
) -> dict:
    """
    对每个(game, feature)估计 median / MAD / scale，返回 stats 字典。
    
    stats = {
      'sym':  {'F1': {'median': 0.72, 'mad': 0.08, 'scale': 0.119, 'ref_n': 4}, ...},
      'maze': {...},
      'circle': {...},
    }
    """

def apply_normalize(
    feature_dicts: List[dict],
    games: List[str],
    stats: dict,
    feature_names: List[str] = ['F1','F2','F3','F4','C1','C2','C3'],
    direction: dict = {'F1':-1,'F2':-1,'F3':+1,'F4':+1,'C1':+1,'C2':+1,'C3':+1},
    clip: Tuple[float,float] = (-3.0, 6.0),
) -> np.ndarray:  # shape (N, 7)
    """
    对每个样本应用 z-score，统一方向为"越大越异常"，裁剪后返回矩阵。
    """
```

**核心公式**（位于 `fit_normalize_stats` 内）：

```python
for g in {'sym', 'maze', 'circle'}:
    for f in feature_names:
        ref_pool = [x[f] for x,game,lab in zip(dicts,games,labels) 
                    if game==g and lab==0 and not is_unanalyzable(x)]
        
        med = np.median(ref_pool)
        mad = np.median(np.abs(np.array(ref_pool) - med))
        scale = 1.4826 * mad                             # 使MAD与正态σ可比
        scale = max(scale, 0.05 * abs(med) + 1e-3)       # 下限，防MAD≈0
        
        stats[g][f] = {'median': med, 'mad': mad, 'scale': scale, 'ref_n': len(ref_pool)}
```

**在 `apply_normalize` 内**：

```python
z_raw = (x - stats[g][f]['median']) / stats[g][f]['scale']
z = direction[f] * z_raw            # F1,F2取负号（正向特征"越大越异常"需反向）
z = np.clip(z, clip[0], clip[1])
```

**关键实现要点**：

1. **MAD下限**：`scale = max(1.4826·MAD, 0.05·|median| + 1e-3)`。sym 游戏只有 4 个正常样本，MAD 可能几乎为 0 导致 z-score 爆炸——这个下限非常关键。
2. **参考池可能很小**：sym 只有 4 个 label=0 样本，circle 只有 5 个，maze 只有 11 个。必要时在 meta 中记录 `ref_n`，若某 `ref_n < 3` 则发出警告（但不终止）。
3. **裁剪范围 `(-3, +6)` 的理由**：左侧 -3 对应"比正常还正常"，继续往左的信息价值已饱和；右侧 +6 保留严重异常样本的强信号。如果发现大量样本被裁到 +6，说明 MAD 估计过小，需要调整下限。
4. **乱画样本不参与归一化的拟合，但也不进入最终的特征矩阵**——它们直接在阶段6被赋予 prob=1.0。

---

### 4.4.3 阶段4.C：主入口 `features/build_feature_matrix.py`

**调用**：
```bash
python features/build_feature_matrix.py \
    --json_dirs output_sym/extract output_maze/extract output_circle/extract \
    --labels data/labels.csv \
    --gate_config configs/gate_thresholds.yaml \
    --out_dir output/
```

**流程**：
```
1. 遍历三个 json_dirs，加载所有样本的 F1..C3 + label + game
2. 调用 gate_unanalyzable → 得到 is_unanalyzable 标记
3. 筛选 {可分析 且 label=0} 样本 → fit_normalize_stats
4. 对 {全部可分析样本} 调用 apply_normalize
5. 输出三个文件：
   - output/feature_matrix.csv       # 可分析样本的z-score矩阵（主分类器输入）
   - output/gate_decisions.csv       # 全部样本的门控结果（阶段6重并入用）
   - output/normalize_stats.json     # 归一化统计量（调试 + 报告用）
```

**输出 `feature_matrix.csv` 格式**：
```csv
sample_id,game,label,F1_z,F2_z,F3_z,F4_z,C1_z,C2_z,C3_z
s1,sym,0,-1.2,-0.8,0.3,-0.5,0.1,-0.2,-0.7
s2,sym,1,2.1,1.8,2.5,1.9,1.2,0.3,1.5
...
```

**初步人工检查**（生成后必做）：
- label=0 样本的 z 值应大致在 [-2, +2] 范围内；
- label=1 样本应在至少 1-2 个特征上出现 z > 2 的明显偏离；
- 若正常样本出现大量 |z| > 3，说明 MAD 下限需要调高；
- 若障碍样本 z 值普遍不高，说明特征判别力不足，可能需要回头调整阶段1-3的特征参数。

---

## 阶段5：分类器 `classifier/classify.py`

### 4.5.1 四个模型的统一接口

```python
class BaseClassifier:
    def fit(self, X_z: np.ndarray, y: np.ndarray, games: List[str]) -> 'BaseClassifier':
        ...
    def predict_proba(self, X_z: np.ndarray) -> np.ndarray:
        """返回异常概率 p ∈ [0,1]（shape=(N,)）"""
        ...
    def get_feature_importance(self) -> Dict[str, float]:
        """返回特征权重（便于跨模型对比）"""
        ...
```

四个实现：`PurePriorScorer` (M2)、`SemiPriorScorer` (M1)、`L2LogisticClassifier` (M3)、`RandomForestClassifier_wrap` (M4)。

---

### 4.5.2 M2：纯先验加权线性评分（消融对照，先写）

**先写 M2 是因为它最简单，可以作为 M1 的骨架**。

```python
W_PRIOR = {
    'F1': 1.2, 'F2': 1.2, 'F3': 1.0, 'F4': 1.0,
    'C1': 0.8, 'C2': 0.8, 'C3': 0.8,
}
# 归一化使 Σw = 7
total = sum(W_PRIOR.values())
W_PRIOR = {k: v * 7 / total for k, v in W_PRIOR.items()}

class PurePriorScorer:
    def fit(self, X_z, y, games):
        self.w_ = np.array([W_PRIOR[f] for f in FEATURE_NAMES])
        return self
    
    def predict_proba(self, X_z):
        score = X_z @ self.w_           # shape=(N,)
        # 用 sigmoid 把分数映射到 [0,1]，斜率参数用训练集标定
        return 1.0 / (1.0 + np.exp(-score / self.sigmoid_scale_))
    
    def get_feature_importance(self):
        return dict(zip(FEATURE_NAMES, self.w_))
```

**sigmoid 缩放参数**：在 `fit` 时计算 `sigmoid_scale_ = std(X_train @ w) + ε`，让 score 在训练集上的标准差为 1 量级，避免 sigmoid 饱和。

### 4.5.3 M1：半先验加权线性评分

```python
class SemiPriorScorer(PurePriorScorer):
    def fit(self, X_z, y, games, bounds=0.3):
        # 从先验开始
        w_init = np.array([W_PRIOR[f] for f in FEATURE_NAMES])
        
        # 定义约束优化：在 w_init * (1±bounds) 的盒约束内最大化 AUROC
        from scipy.optimize import minimize
        
        def neg_auroc(w):
            score = X_z @ w
            # 近似AUROC（Wilcoxon-Mann-Whitney统计量）
            pos = score[y==1]; neg = score[y==0]
            if len(pos)==0 or len(neg)==0: return 0.0
            return -np.mean(pos[:,None] > neg[None,:])
        
        lo = w_init * (1 - bounds)
        hi = w_init * (1 + bounds)
        res = minimize(neg_auroc, w_init, method='L-BFGS-B',
                       bounds=list(zip(lo, hi)))
        self.w_ = res.x
        # 后续同 PurePriorScorer
```

**边界值 0.3 的含义**：每个特征权重在先验值的 ±30% 内微调。如果数据强烈主张某特征的权重"应该"更高或更低，优化结果会贴到边界上——这本身就是一个可报告的发现。

### 4.5.4 M3：L2 Logistic Regression（主力）

```python
from sklearn.linear_model import LogisticRegression

class L2LogisticClassifier:
    def __init__(self, C=1.0):
        self.C = C
    
    def fit(self, X_z, y, games):
        self.model_ = LogisticRegression(
            penalty='l2', C=self.C,
            class_weight='balanced',     # 类别不平衡
            max_iter=1000, solver='lbfgs',
        )
        self.model_.fit(X_z, y)
        return self
    
    def predict_proba(self, X_z):
        return self.model_.predict_proba(X_z)[:, 1]
    
    def get_feature_importance(self):
        return dict(zip(FEATURE_NAMES, self.model_.coef_[0]))
```

**C 参数的选择**：在阶段6的LOSO循环内嵌套一个小的网格搜索 `C ∈ {0.1, 0.3, 1.0, 3.0}`，用内层LOO选最优。考虑到样本量小，嵌套CV会让评估时间变长但结果更诚实。若时间紧张，直接用 `C=1.0` 并在报告中说明。

### 4.5.5 M4：Random Forest（对照）

```python
from sklearn.ensemble import RandomForestClassifier

class RandomForestClassifier_wrap:
    def fit(self, X_z, y, games):
        self.model_ = RandomForestClassifier(
            n_estimators=200,
            max_depth=4,               # 浅树
            min_samples_leaf=3,
            class_weight='balanced',
            random_state=42,
        )
        self.model_.fit(X_z, y)
        return self
    
    def predict_proba(self, X_z):
        return self.model_.predict_proba(X_z)[:, 1]
    
    def get_feature_importance(self):
        return dict(zip(FEATURE_NAMES, self.model_.feature_importances_))
```

**作用**：**不是为了打败 M3，而是为了证明"非线性模型并未显著提升"，从而支持"简单模型已够"的方法论主张**。如果 M4 意外地显著优于 M3，说明决策边界非线性，需要反思 LR 是否足够。

---

## 阶段6：评估 `experiments/run_experiments.py`

### 4.6.1 LOSO 主流程

```bash
python experiments/run_experiments.py \
    --feature_matrix output/feature_matrix.csv \
    --gate_decisions output/gate_decisions.csv \
    --out_dir results/
```

**核心循环**（伪代码）：

```python
# 读可分析样本
df_analyzable = load(feature_matrix.csv)    # 乱画样本不在此
# 读门控结果（含乱画）
df_gate = load(gate_decisions.csv)

N = len(df_analyzable)
models = {'M1':SemiPriorScorer(), 'M2':PurePriorScorer(),
          'M3':L2LogisticClassifier(), 'M4':RandomForestClassifier_wrap()}

# 每个模型的每个样本一个预测概率
probs = {m: np.zeros(N) for m in models}

for i in range(N):
    train_idx = [j for j in range(N) if j != i]
    test_idx  = [i]
    
    # 【关键】归一化统计量只在 train 上拟合
    # 但feature_matrix.csv已经是归一化后的，这意味着此处有数据泄漏！
    # ↓ 所以正确做法见下一小节
    
    for mname, model in models.items():
        model.fit(X_z[train_idx], y[train_idx], games[train_idx])
        probs[mname][i] = model.predict_proba(X_z[test_idx])[0]
```

### 4.6.2 严格防泄漏的正确实现

上面的简化流程有一个严重问题：`feature_matrix.csv` 是用**全部可分析样本**（含测试样本）的统计量算的 z-score。测试样本参与了自己的归一化——数据泄漏。

**正确的 LOSO 需要保存原始特征，每折重新归一化**：

```python
# 读原始特征（未归一化）
df_raw = load_all_raw_features(json_dirs)
df_raw = df_raw[df_raw['is_unanalyzable'] == False]   # 只留可分析样本

X_raw = df_raw[FEATURE_NAMES].values
y = df_raw['label'].values
games = df_raw['game'].values

for i in range(N):
    train_idx = [j for j in range(N) if j != i]
    test_idx  = [i]
    
    # 每折重新拟合归一化
    stats = fit_normalize_stats(
        feature_dicts=X_raw[train_idx], 
        games=games[train_idx],
        labels=y[train_idx],
    )
    X_train_z = apply_normalize(X_raw[train_idx], games[train_idx], stats)
    X_test_z  = apply_normalize(X_raw[test_idx],  games[test_idx],  stats)
    
    for mname, model in models.items():
        model.fit(X_train_z, y[train_idx], games[train_idx])
        probs[mname][i] = model.predict_proba(X_test_z)[0]
```

**注意**：归一化参考池是 train 中的 label=0 样本。如果 test 样本恰好是某游戏的 label=0 样本之一，train 中该游戏的参考池会少一个样本——这会让 sym 的 label=0 池从 4 降到 3，MAD 估计更不稳定。**阶段4.B 的 MAD下限保护机制此时非常关键**。

### 4.6.3 乱画样本并入终判

```python
# LOSO结束后，对每个乱画样本直接赋异常概率
unanalyzable_ids = df_gate[df_gate['is_unanalyzable']==True]['sample_id']

# 构造全样本的 prob 和 y
probs_full = {m: [] for m in models}
y_full = []
games_full = []

for row in df_gate.iterrows():
    sid = row['sample_id']
    y_full.append(row['label'])
    games_full.append(row['game'])
    if row['is_unanalyzable']:
        for m in models: probs_full[m].append(1.0)     # 门控判异常
    else:
        idx = df_analyzable.index[df_analyzable['sample_id']==sid][0]
        for m in models: probs_full[m].append(probs[m][idx])
```

**所有评估指标都基于 `probs_full` 和 `y_full` 计算**——这反映的是完整筛查系统的性能。

### 4.6.4 评估指标与报告

**三层指标表**：

```
【主任务性能】（在 全部样本 上计算）
                    AUROC    AUPRC    F1@opt    阈值@opt
M2 (纯先验)        0.xxx    0.xxx    0.xxx     0.xx
M1 (半先验)        0.xxx    0.xxx    0.xxx     0.xx
M3 (L2 LR)        0.xxx    0.xxx    0.xxx     0.xx
M4 (RF)           0.xxx    0.xxx    0.xxx     0.xx

【门控诊断】
已知乱画样本数：12
门控召回：xx/12
门控误判（正常样本被门控）：x
门控未识别的乱画（进入主分类器）：x

【per-game AUROC 拆分】（仅主力 M3）
sym:    0.xxx  (n=7+1乱画)
maze:   0.xxx  (n=29+5乱画)
circle: 0.xxx  (n=15+6乱画)
```

**关键对比叙事**：
- M2 → M1：数据微调的边际价值
- M1 → M3：软约束（权重盒约束）vs 硬约束（L2正则）
- M3 → M4：非线性模型的增益
- 若 M3 ≈ M4：**"本任务接近线性可分，简单模型已足够"——这是核心结论**

### 4.6.5 可解释性图表

**特征权重对比条形图**：

```
            F1   F2   F3   F4   C1   C2   C3
M1 w_final  1.3  1.5  1.0  0.8  0.6  0.4  0.9    ← 半先验微调
M2 w_prior  1.2  1.2  1.0  1.0  0.8  0.8  0.8    ← 纯先验（不变）
M3 |coef|   0.9  1.2  0.7  0.6  0.3  0.1  0.8    ← 数据驱动
M4 impor.   0.18 0.21 0.15 0.12 0.08 0.05 0.21   ← 树模型
```

**跨模型一致性分析**：计算四组权重的 **Spearman 秩相关系数**，若 ≥ 0.7 说明特征重要性排序在不同模型间一致，是方法论稳健性的有力证据。

### 4.6.6 暂不做但可选的加分项

1. **LOGO（Leave-One-Game-Out）评估**：根据你的选择，**先不做，等 LOSO 结果出来再决定**。如果 LOSO 上 AUROC ≥ 0.8 且跨模型一致，再加 LOGO 验证跨游戏泛化。
2. **归一化消融**：跑一次"不做归一化 / 不做per-game 归一化"的基线，对比 AUROC 下降量——这是归一化方法论贡献的直接证据。
3. **SHAP 值**：对 M3 做样本级的 SHAP 解释，展示具体某个样本为什么被判为异常。对方法论不是必需的，但放报告里很有说服力。

---

## 输出文件清单

```
output/
├─ gate_decisions.csv              # 全部样本门控结果
├─ gate_validation_report.txt      # 门控阈值验证报告
├─ feature_matrix.csv              # 可分析样本的z-score矩阵
└─ normalize_stats.json            # 归一化统计量

results/
├─ loso_predictions.csv            # 每个样本在四个模型下的prob
├─ metrics_summary.csv             # 主任务指标表
├─ per_game_metrics.csv            # per-game AUROC拆分
├─ feature_weights_comparison.csv  # 四个模型的特征权重
└─ figures/
   ├─ weight_barplot.png           # 特征权重条形图
   ├─ roc_curves.png               # 四个模型的ROC曲线
   └─ pergame_roc.png              # M3的三个游戏ROC
```

---

## 下一步动作

按你选的顺序推进：

1. **先写 `features/gate_unanalyzable.py`**，用初始阈值跑一遍，生成 `gate_validation_report.txt`。
2. **把报告内容贴给我**（包括12个已知乱画样本的F1/F2/F4值、是否被正确识别、label=0样本的极值对照），我帮你决定是否需要回调阈值。
3. **阈值确定后再继续 normalize 和 classify**。

需要我现在开始写 `gate_unanalyzable.py` 的完整代码吗？还是你想先把这个v3.1方案并入文档、自己对照着实现？